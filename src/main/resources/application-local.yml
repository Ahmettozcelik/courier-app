spring:
  kafka:
    bootstrap-servers: localhost:9092

  data:
    mongodb:
      host: localhost
      port: 27017
      database: tracking
    redis:
      host: localhost
      port: 6379
      timeout: 2000
