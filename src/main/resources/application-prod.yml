spring:
  kafka:
    bootstrap-servers: kafka:29092

  data:
    mongodb:
      host: mongodb
      port: 27017
      database: tracking
    redis:
      host: redis
      port: 6379
      timeout: 2000
